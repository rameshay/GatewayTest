
/*
 * Multi users using multiple robots
 */


// SimpleContent defines properties of content that the server generates;
// if you get no hits, set SimpleContent.obj_life_cycle to cntStatic, which
// is defined in workloads/include/contents.pg
Content SimpleContent = {
        size = const(5KB); // response sizes distributed constantly
        //size = const(6Byte);
        cachable = 80%;   // 20% of content is uncachable
};


AddrMap map = {
           addresses = [ '10.0.15.60:443' ];
           names = [ 'www.dropbox.com'];
};

use (map);


DnsResolver dr = {
    servers = [ '10.0.15.60:53' ];
    timeout = 5sec;
};


SslWrap wrap = {
        protocols = [ "any" ];
        ciphers = [ "ALL:!DES-CBC-SHA:!EXP-DES-CBC-SHA:!EXP-RC4-MD5:!EXP-RC2-CBC-MD5:" : 100% ];

        rsa_key_sizes = [ 512bit, 1024bit, 2048bit ];
        session_resumption = 40%;
        session_cache = 100;
        verify_peer_certificate = false;
};


// a primitive server cleverly labeled "S101"
// normally, you would specify more properties,
// but we will mostly rely on defaults for now
Server S = {
	kind = "S101";
	contents = [ SimpleContent ];
	direct_access = contents;
	addresses = ['10.0.15.60:443' ]; // where to create these server agents
	ssl_wraps = [ wrap ];
};


Phase phRampUp = { name = "rampup"; goal.duration = 5min; populus_factor_beg = 0; populus_factor_end = 1;};
Phase phRampDown = { name = "rampdown"; goal.duration = 5min; populus_factor_beg = 1; populus_factor_end = 0;};
Phase phSustain = { name = "sustain"; goal.duration = 60min; populus_factor_beg = 1; populus_factor_end = 1;};

// build schedule using some well-known phases and phases defined above
//schedule(phRampUp,
//         phSustain,
//         phRampDown);

schedule(phRampUp,
         phSustain);

// robots

Robot R1 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user1 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user1@elastica.co';
        http_headers = [user1: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R2 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user2 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user2@elastica.co';
        http_headers = [user2: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R3 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user3 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user3@elastica.co';
        http_headers = [user3: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R4 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user4 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user4@elastica.co';
        http_headers = [user4: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R5 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user5 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user5@elastica.co';
        http_headers = [user5: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R6 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user6 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user6@elastica.co';
        http_headers = [user6: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R7 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user7 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user7@elastica.co';
        http_headers = [user7: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R8 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user8 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user8@elastica.co';
        http_headers = [user8: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R9 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user9 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user9@elastica.co';
        http_headers = [user9: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R10 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user10 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user10@elastica.co';
        http_headers = [user10: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R11 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user11 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user11@elastica.co';
        http_headers = [user11: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R12 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user12 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user12@elastica.co';
        http_headers = [user12: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R13 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user13 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user13@elastica.co';
        http_headers = [user13: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R14 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user14 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user14@elastica.co';
        http_headers = [user14: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R15 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user15 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user15@elastica.co';
        http_headers = [user15: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R16 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user16 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user16@elastica.co';
        http_headers = [user16: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R17 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user17 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user17@elastica.co';
        http_headers = [user17: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R18 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user18 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user18@elastica.co';
        http_headers = [user18: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R19 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user19 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user19@elastica.co';
        http_headers = [user19: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R20 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user20 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user20@elastica.co';
        http_headers = [user20: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R21 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user21 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user21@elastica.co';
        http_headers = [user21: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R22 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user22 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user22@elastica.co';
        http_headers = [user22: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R23 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user23 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user23@elastica.co';
        http_headers = [user23: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R24 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user24 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user24@elastica.co';
        http_headers = [user24: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R25 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user25 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user25@elastica.co';
        http_headers = [user25: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R26 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user26 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user26@elastica.co';
        http_headers = [user26: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R27 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user27 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user27@elastica.co';
        http_headers = [user27: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R28 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user28 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user28@elastica.co';
        http_headers = [user28: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R29 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user29 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user29@elastica.co';
        http_headers = [user29: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R30 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user30 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user30@elastica.co';
        http_headers = [user30: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R31 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user31 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user31@elastica.co';
        http_headers = [user31: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R32 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user32 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user32@elastica.co';
        http_headers = [user32: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R33 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user33 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user33@elastica.co';
        http_headers = [user33: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R34 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user34 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user34@elastica.co';
        http_headers = [user34: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R35 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user35 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user35@elastica.co';
        http_headers = [user35: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R36 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user36 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user36@elastica.co';
        http_headers = [user36: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R37 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user37 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user37@elastica.co';
        http_headers = [user37: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R38 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user38 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user38@elastica.co';
        http_headers = [user38: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R39 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user39 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user39@elastica.co';
        http_headers = [user39: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R40 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user40 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user40@elastica.co';
        http_headers = [user40: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R41 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user41 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user41@elastica.co';
        http_headers = [user41: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R42 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user42 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user42@elastica.co';
        http_headers = [user42: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R43 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user43 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user43@elastica.co';
        http_headers = [user43: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R44 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user44 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user44@elastica.co';
        http_headers = [user44: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R45 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user45 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user45@elastica.co';
        http_headers = [user45: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R46 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user46 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user46@elastica.co';
        http_headers = [user46: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R47 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user47 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user47@elastica.co';
        http_headers = [user47: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R48 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user48 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user48@elastica.co';
        http_headers = [user48: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R49 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user49 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user49@elastica.co';
        http_headers = [user49: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R50 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user50 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user50@elastica.co';
        http_headers = [user50: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R51 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user51 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user51@elastica.co';
        http_headers = [user51: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R52 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user52 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user52@elastica.co';
        http_headers = [user52: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R53 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user53 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user53@elastica.co';
        http_headers = [user53: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R54 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user54 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user54@elastica.co';
        http_headers = [user54: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R55 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user55 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user55@elastica.co';
        http_headers = [user55: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R56 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user56 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user56@elastica.co';
        http_headers = [user56: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R57 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user57 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user57@elastica.co';
        http_headers = [user57: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R58 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user58 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user58@elastica.co';
        http_headers = [user58: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R59 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user59 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user59@elastica.co';
        http_headers = [user59: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R60 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user60 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user60@elastica.co';
        http_headers = [user60: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R61 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user61 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user61@elastica.co';
        http_headers = [user61: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R62 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user62 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user62@elastica.co';
        http_headers = [user62: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R63 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user63 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user63@elastica.co';
        http_headers = [user63: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R64 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user64 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user64@elastica.co';
        http_headers = [user64: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R65 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user65 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user65@elastica.co';
        http_headers = [user65: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R66 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user66 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user66@elastica.co';
        http_headers = [user66: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R67 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user67 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user67@elastica.co';
        http_headers = [user67: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R68 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user68 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user68@elastica.co';
        http_headers = [user68: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R69 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user69 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user69@elastica.co';
        http_headers = [user69: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R70 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user70 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user70@elastica.co';
        http_headers = [user70: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R71 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user71 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user71@elastica.co';
        http_headers = [user71: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R72 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user72 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user72@elastica.co';
        http_headers = [user72: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R73 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user73 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user73@elastica.co';
        http_headers = [user73: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R74 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user74 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user74@elastica.co';
        http_headers = [user74: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R75 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user75 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user75@elastica.co';
        http_headers = [user75: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R76 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user76 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user76@elastica.co';
        http_headers = [user76: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R77 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user77 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user77@elastica.co';
        http_headers = [user77: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R78 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user78 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user78@elastica.co';
        http_headers = [user78: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R79 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user79 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user79@elastica.co';
        http_headers = [user79: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R80 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user80 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user80@elastica.co';
        http_headers = [user80: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R81 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user81 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user81@elastica.co';
        http_headers = [user81: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R82 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user82 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user82@elastica.co';
        http_headers = [user82: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R83 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user83 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user83@elastica.co';
        http_headers = [user83: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R84 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user84 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user84@elastica.co';
        http_headers = [user84: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R85 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user85 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user85@elastica.co';
        http_headers = [user85: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R86 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user86 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user86@elastica.co';
        http_headers = [user86: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R87 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user87 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user87@elastica.co';
        http_headers = [user87: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R88 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user88 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user88@elastica.co';
        http_headers = [user88: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R89 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user89 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user89@elastica.co';
        http_headers = [user89: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R90 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user90 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user90@elastica.co';
        http_headers = [user90: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R91 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user91 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user91@elastica.co';
        http_headers = [user91: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R92 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user92 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user92@elastica.co';
        http_headers = [user92: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R93 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user93 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user93@elastica.co';
        http_headers = [user93: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R94 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user94 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user94@elastica.co';
        http_headers = [user94: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R95 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user95 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user95@elastica.co';
        http_headers = [user95: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R96 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user96 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user96@elastica.co';
        http_headers = [user96: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R97 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user97 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user97@elastica.co';
        http_headers = [user97: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R98 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user98 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user98@elastica.co';
        http_headers = [user98: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R99 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user99 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user99@elastica.co';
        http_headers = [user99: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R100 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user100 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user100@elastica.co';
        http_headers = [user100: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R101 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user101 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user101@elastica.co';
        http_headers = [user101: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R102 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user102 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user102@elastica.co';
        http_headers = [user102: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R103 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user103 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user103@elastica.co';
        http_headers = [user103: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R104 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user104 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user104@elastica.co';
        http_headers = [user104: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R105 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user105 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user105@elastica.co';
        http_headers = [user105: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R106 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user106 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user106@elastica.co';
        http_headers = [user106: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R107 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user107 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user107@elastica.co';
        http_headers = [user107: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R108 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user108 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user108@elastica.co';
        http_headers = [user108: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R109 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user109 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user109@elastica.co';
        http_headers = [user109: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R110 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user110 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user110@elastica.co';
        http_headers = [user110: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R111 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user111 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user111@elastica.co';
        http_headers = [user111: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R112 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user112 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user112@elastica.co';
        http_headers = [user112: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R113 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user113 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user113@elastica.co';
        http_headers = [user113: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R114 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user114 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user114@elastica.co';
        http_headers = [user114: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R115 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user115 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user115@elastica.co';
        http_headers = [user115: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R116 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user116 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user116@elastica.co';
        http_headers = [user116: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R117 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user117 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user117@elastica.co';
        http_headers = [user117: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R118 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user118 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user118@elastica.co';
        http_headers = [user118: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R119 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user119 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user119@elastica.co';
        http_headers = [user119: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R120 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user120 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user120@elastica.co';
        http_headers = [user120: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R121 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user121 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user121@elastica.co';
        http_headers = [user121: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R122 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user122 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user122@elastica.co';
        http_headers = [user122: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R123 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user123 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user123@elastica.co';
        http_headers = [user123: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R124 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user124 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user124@elastica.co';
        http_headers = [user124: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R125 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user125 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user125@elastica.co';
        http_headers = [user125: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R126 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user126 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user126@elastica.co';
        http_headers = [user126: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R127 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user127 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user127@elastica.co';
        http_headers = [user127: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R128 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user128 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user128@elastica.co';
        http_headers = [user128: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R129 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user129 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user129@elastica.co';
        http_headers = [user129: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R130 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user130 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user130@elastica.co';
        http_headers = [user130: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R131 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user131 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user131@elastica.co';
        http_headers = [user131: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R132 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user132 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user132@elastica.co';
        http_headers = [user132: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R133 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user133 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user133@elastica.co';
        http_headers = [user133: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R134 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user134 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user134@elastica.co';
        http_headers = [user134: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R135 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user135 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user135@elastica.co';
        http_headers = [user135: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R136 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user136 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user136@elastica.co';
        http_headers = [user136: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R137 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user137 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user137@elastica.co';
        http_headers = [user137: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R138 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user138 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user138@elastica.co';
        http_headers = [user138: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R139 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user139 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user139@elastica.co';
        http_headers = [user139: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R140 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user140 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user140@elastica.co';
        http_headers = [user140: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R141 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user141 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user141@elastica.co';
        http_headers = [user141: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R142 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user142 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user142@elastica.co';
        http_headers = [user142: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R143 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user143 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user143@elastica.co';
        http_headers = [user143: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R144 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user144 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user144@elastica.co';
        http_headers = [user144: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R145 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user145 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user145@elastica.co';
        http_headers = [user145: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R146 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user146 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user146@elastica.co';
        http_headers = [user146: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R147 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user147 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user147@elastica.co';
        http_headers = [user147: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R148 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user148 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user148@elastica.co';
        http_headers = [user148: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R149 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user149 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user149@elastica.co';
        http_headers = [user149: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R150 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user150 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user150@elastica.co';
        http_headers = [user150: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R151 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user151 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user151@elastica.co';
        http_headers = [user151: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R152 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user152 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user152@elastica.co';
        http_headers = [user152: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R153 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user153 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user153@elastica.co';
        http_headers = [user153: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R154 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user154 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user154@elastica.co';
        http_headers = [user154: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R155 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user155 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user155@elastica.co';
        http_headers = [user155: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R156 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user156 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user156@elastica.co';
        http_headers = [user156: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R157 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user157 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user157@elastica.co';
        http_headers = [user157: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R158 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user158 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user158@elastica.co';
        http_headers = [user158: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R159 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user159 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user159@elastica.co';
        http_headers = [user159: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R160 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user160 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user160@elastica.co';
        http_headers = [user160: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R161 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user161 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user161@elastica.co';
        http_headers = [user161: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R162 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user162 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user162@elastica.co';
        http_headers = [user162: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R163 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user163 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user163@elastica.co';
        http_headers = [user163: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R164 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user164 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user164@elastica.co';
        http_headers = [user164: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R165 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user165 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user165@elastica.co';
        http_headers = [user165: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R166 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user166 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user166@elastica.co';
        http_headers = [user166: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R167 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user167 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user167@elastica.co';
        http_headers = [user167: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R168 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user168 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user168@elastica.co';
        http_headers = [user168: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R169 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user169 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user169@elastica.co';
        http_headers = [user169: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R170 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user170 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user170@elastica.co';
        http_headers = [user170: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R171 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user171 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user171@elastica.co';
        http_headers = [user171: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R172 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user172 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user172@elastica.co';
        http_headers = [user172: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R173 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user173 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user173@elastica.co';
        http_headers = [user173: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R174 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user174 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user174@elastica.co';
        http_headers = [user174: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R175 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user175 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user175@elastica.co';
        http_headers = [user175: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R176 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user176 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user176@elastica.co';
        http_headers = [user176: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R177 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user177 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user177@elastica.co';
        http_headers = [user177: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R178 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user178 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user178@elastica.co';
        http_headers = [user178: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R179 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user179 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user179@elastica.co';
        http_headers = [user179: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R180 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user180 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user180@elastica.co';
        http_headers = [user180: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R181 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user181 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user181@elastica.co';
        http_headers = [user181: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R182 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user182 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user182@elastica.co';
        http_headers = [user182: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R183 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user183 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user183@elastica.co';
        http_headers = [user183: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R184 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user184 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user184@elastica.co';
        http_headers = [user184: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R185 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user185 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user185@elastica.co';
        http_headers = [user185: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R186 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user186 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user186@elastica.co';
        http_headers = [user186: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R187 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user187 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user187@elastica.co';
        http_headers = [user187: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R188 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user188 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user188@elastica.co';
        http_headers = [user188: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R189 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user189 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user189@elastica.co';
        http_headers = [user189: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R190 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user190 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user190@elastica.co';
        http_headers = [user190: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R191 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user191 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user191@elastica.co';
        http_headers = [user191: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R192 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user192 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user192@elastica.co';
        http_headers = [user192: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R193 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user193 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user193@elastica.co';
        http_headers = [user193: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R194 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user194 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user194@elastica.co';
        http_headers = [user194: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R195 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user195 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user195@elastica.co';
        http_headers = [user195: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R196 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user196 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user196@elastica.co';
        http_headers = [user196: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R197 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user197 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user197@elastica.co';
        http_headers = [user197: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R198 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user198 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user198@elastica.co';
        http_headers = [user198: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R199 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user199 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user199@elastica.co';
        http_headers = [user199: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R200 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user200 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user200@elastica.co';
        http_headers = [user200: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R201 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user201 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user201@elastica.co';
        http_headers = [user201: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R202 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user202 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user202@elastica.co';
        http_headers = [user202: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R203 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user203 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user203@elastica.co';
        http_headers = [user203: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R204 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user204 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user204@elastica.co';
        http_headers = [user204: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R205 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user205 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user205@elastica.co';
        http_headers = [user205: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R206 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user206 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user206@elastica.co';
        http_headers = [user206: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R207 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user207 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user207@elastica.co';
        http_headers = [user207: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R208 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user208 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user208@elastica.co';
        http_headers = [user208: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R209 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user209 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user209@elastica.co';
        http_headers = [user209: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R210 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user210 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user210@elastica.co';
        http_headers = [user210: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R211 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user211 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user211@elastica.co';
        http_headers = [user211: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R212 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user212 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user212@elastica.co';
        http_headers = [user212: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R213 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user213 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user213@elastica.co';
        http_headers = [user213: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R214 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user214 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user214@elastica.co';
        http_headers = [user214: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R215 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user215 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user215@elastica.co';
        http_headers = [user215: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R216 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user216 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user216@elastica.co';
        http_headers = [user216: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R217 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user217 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user217@elastica.co';
        http_headers = [user217: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R218 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user218 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user218@elastica.co';
        http_headers = [user218: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R219 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user219 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user219@elastica.co';
        http_headers = [user219: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R220 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user220 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user220@elastica.co';
        http_headers = [user220: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R221 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user221 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user221@elastica.co';
        http_headers = [user221: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R222 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user222 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user222@elastica.co';
        http_headers = [user222: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R223 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user223 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user223@elastica.co';
        http_headers = [user223: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R224 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user224 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user224@elastica.co';
        http_headers = [user224: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R225 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user225 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user225@elastica.co';
        http_headers = [user225: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R226 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user226 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user226@elastica.co';
        http_headers = [user226: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R227 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user227 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user227@elastica.co';
        http_headers = [user227: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R228 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user228 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user228@elastica.co';
        http_headers = [user228: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R229 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user229 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user229@elastica.co';
        http_headers = [user229: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R230 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user230 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user230@elastica.co';
        http_headers = [user230: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R231 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user231 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user231@elastica.co';
        http_headers = [user231: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R232 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user232 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user232@elastica.co';
        http_headers = [user232: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R233 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user233 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user233@elastica.co';
        http_headers = [user233: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R234 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user234 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user234@elastica.co';
        http_headers = [user234: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R235 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user235 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user235@elastica.co';
        http_headers = [user235: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R236 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user236 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user236@elastica.co';
        http_headers = [user236: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R237 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user237 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user237@elastica.co';
        http_headers = [user237: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R238 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user238 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user238@elastica.co';
        http_headers = [user238: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R239 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user239 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user239@elastica.co';
        http_headers = [user239: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R240 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user240 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user240@elastica.co';
        http_headers = [user240: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R241 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user241 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user241@elastica.co';
        http_headers = [user241: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R242 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user242 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user242@elastica.co';
        http_headers = [user242: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R243 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user243 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user243@elastica.co';
        http_headers = [user243: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R244 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user244 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user244@elastica.co';
        http_headers = [user244: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R245 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user245 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user245@elastica.co';
        http_headers = [user245: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R246 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user246 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user246@elastica.co';
        http_headers = [user246: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R247 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user247 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user247@elastica.co';
        http_headers = [user247: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R248 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user248 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user248@elastica.co';
        http_headers = [user248: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R249 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user249 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user249@elastica.co';
        http_headers = [user249: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R250 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user250 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user250@elastica.co';
        http_headers = [user250: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R251 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user251 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user251@elastica.co';
        http_headers = [user251: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R252 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user252 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user252@elastica.co';
        http_headers = [user252: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R253 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user253 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user253@elastica.co';
        http_headers = [user253: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R254 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user254 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user254@elastica.co';
        http_headers = [user254: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R255 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user255 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user255@elastica.co';
        http_headers = [user255: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R256 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user256 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user256@elastica.co';
        http_headers = [user256: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R257 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user257 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user257@elastica.co';
        http_headers = [user257: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R258 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user258 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user258@elastica.co';
        http_headers = [user258: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R259 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user259 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user259@elastica.co';
        http_headers = [user259: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R260 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user260 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user260@elastica.co';
        http_headers = [user260: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R261 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user261 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user261@elastica.co';
        http_headers = [user261: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R262 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user262 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user262@elastica.co';
        http_headers = [user262: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R263 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user263 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user263@elastica.co';
        http_headers = [user263: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R264 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user264 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user264@elastica.co';
        http_headers = [user264: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R265 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user265 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user265@elastica.co';
        http_headers = [user265: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R266 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user266 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user266@elastica.co';
        http_headers = [user266: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R267 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user267 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user267@elastica.co';
        http_headers = [user267: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R268 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user268 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user268@elastica.co';
        http_headers = [user268: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R269 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user269 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user269@elastica.co';
        http_headers = [user269: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R270 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user270 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user270@elastica.co';
        http_headers = [user270: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R271 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user271 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user271@elastica.co';
        http_headers = [user271: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R272 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user272 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user272@elastica.co';
        http_headers = [user272: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R273 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user273 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user273@elastica.co';
        http_headers = [user273: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R274 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user274 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user274@elastica.co';
        http_headers = [user274: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R275 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user275 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user275@elastica.co';
        http_headers = [user275: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R276 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user276 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user276@elastica.co';
        http_headers = [user276: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R277 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user277 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user277@elastica.co';
        http_headers = [user277: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R278 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user278 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user278@elastica.co';
        http_headers = [user278: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R279 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user279 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user279@elastica.co';
        http_headers = [user279: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R280 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user280 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user280@elastica.co';
        http_headers = [user280: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R281 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user281 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user281@elastica.co';
        http_headers = [user281: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R282 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user282 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user282@elastica.co';
        http_headers = [user282: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R283 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user283 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user283@elastica.co';
        http_headers = [user283: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R284 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user284 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user284@elastica.co';
        http_headers = [user284: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R285 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user285 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user285@elastica.co';
        http_headers = [user285: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R286 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user286 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user286@elastica.co';
        http_headers = [user286: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R287 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user287 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user287@elastica.co';
        http_headers = [user287: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R288 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user288 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user288@elastica.co';
        http_headers = [user288: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R289 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user289 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user289@elastica.co';
        http_headers = [user289: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R290 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user290 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user290@elastica.co';
        http_headers = [user290: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R291 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user291 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user291@elastica.co';
        http_headers = [user291: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R292 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user292 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user292@elastica.co';
        http_headers = [user292: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R293 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user293 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user293@elastica.co';
        http_headers = [user293: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R294 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user294 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user294@elastica.co';
        http_headers = [user294: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R295 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user295 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user295@elastica.co';
        http_headers = [user295: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R296 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user296 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user296@elastica.co';
        http_headers = [user296: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R297 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user297 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user297@elastica.co';
        http_headers = [user297: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R298 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user298 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user298@elastica.co';
        http_headers = [user298: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R299 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user299 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user299@elastica.co';
        http_headers = [user299: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R300 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user300 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user300@elastica.co';
        http_headers = [user300: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R301 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user301 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user301@elastica.co';
        http_headers = [user301: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R302 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user302 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user302@elastica.co';
        http_headers = [user302: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R303 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user303 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user303@elastica.co';
        http_headers = [user303: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R304 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user304 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user304@elastica.co';
        http_headers = [user304: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R305 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user305 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user305@elastica.co';
        http_headers = [user305: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R306 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user306 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user306@elastica.co';
        http_headers = [user306: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R307 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user307 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user307@elastica.co';
        http_headers = [user307: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R308 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user308 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user308@elastica.co';
        http_headers = [user308: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R309 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user309 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user309@elastica.co';
        http_headers = [user309: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R310 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user310 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user310@elastica.co';
        http_headers = [user310: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R311 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user311 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user311@elastica.co';
        http_headers = [user311: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R312 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user312 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user312@elastica.co';
        http_headers = [user312: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R313 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user313 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user313@elastica.co';
        http_headers = [user313: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R314 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user314 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user314@elastica.co';
        http_headers = [user314: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R315 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user315 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user315@elastica.co';
        http_headers = [user315: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R316 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user316 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user316@elastica.co';
        http_headers = [user316: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R317 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user317 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user317@elastica.co';
        http_headers = [user317: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R318 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user318 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user318@elastica.co';
        http_headers = [user318: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R319 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user319 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user319@elastica.co';
        http_headers = [user319: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R320 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user320 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user320@elastica.co';
        http_headers = [user320: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R321 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user321 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user321@elastica.co';
        http_headers = [user321: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R322 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user322 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user322@elastica.co';
        http_headers = [user322: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R323 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user323 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user323@elastica.co';
        http_headers = [user323: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R324 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user324 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user324@elastica.co';
        http_headers = [user324: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R325 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user325 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user325@elastica.co';
        http_headers = [user325: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R326 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user326 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user326@elastica.co';
        http_headers = [user326: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R327 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user327 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user327@elastica.co';
        http_headers = [user327: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R328 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user328 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user328@elastica.co';
        http_headers = [user328: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R329 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user329 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user329@elastica.co';
        http_headers = [user329: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R330 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user330 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user330@elastica.co';
        http_headers = [user330: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R331 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user331 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user331@elastica.co';
        http_headers = [user331: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R332 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user332 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user332@elastica.co';
        http_headers = [user332: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R333 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user333 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user333@elastica.co';
        http_headers = [user333: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R334 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user334 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user334@elastica.co';
        http_headers = [user334: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R335 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user335 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user335@elastica.co';
        http_headers = [user335: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R336 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user336 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user336@elastica.co';
        http_headers = [user336: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R337 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user337 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user337@elastica.co';
        http_headers = [user337: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R338 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user338 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user338@elastica.co';
        http_headers = [user338: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R339 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user339 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user339@elastica.co';
        http_headers = [user339: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R340 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user340 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user340@elastica.co';
        http_headers = [user340: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R341 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user341 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user341@elastica.co';
        http_headers = [user341: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R342 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user342 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user342@elastica.co';
        http_headers = [user342: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R343 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user343 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user343@elastica.co';
        http_headers = [user343: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R344 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user344 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user344@elastica.co';
        http_headers = [user344: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R345 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user345 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user345@elastica.co';
        http_headers = [user345: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R346 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user346 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user346@elastica.co';
        http_headers = [user346: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R347 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user347 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user347@elastica.co';
        http_headers = [user347: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R348 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user348 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user348@elastica.co';
        http_headers = [user348: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R349 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user349 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user349@elastica.co';
        http_headers = [user349: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R350 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user350 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user350@elastica.co';
        http_headers = [user350: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R351 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user351 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user351@elastica.co';
        http_headers = [user351: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R352 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user352 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user352@elastica.co';
        http_headers = [user352: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R353 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user353 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user353@elastica.co';
        http_headers = [user353: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R354 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user354 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user354@elastica.co';
        http_headers = [user354: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R355 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user355 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user355@elastica.co';
        http_headers = [user355: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R356 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user356 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user356@elastica.co';
        http_headers = [user356: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R357 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user357 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user357@elastica.co';
        http_headers = [user357: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R358 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user358 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user358@elastica.co';
        http_headers = [user358: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R359 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user359 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user359@elastica.co';
        http_headers = [user359: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R360 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user360 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user360@elastica.co';
        http_headers = [user360: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R361 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user361 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user361@elastica.co';
        http_headers = [user361: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R362 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user362 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user362@elastica.co';
        http_headers = [user362: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R363 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user363 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user363@elastica.co';
        http_headers = [user363: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R364 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user364 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user364@elastica.co';
        http_headers = [user364: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R365 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user365 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user365@elastica.co';
        http_headers = [user365: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R366 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user366 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user366@elastica.co';
        http_headers = [user366: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R367 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user367 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user367@elastica.co';
        http_headers = [user367: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R368 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user368 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user368@elastica.co';
        http_headers = [user368: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R369 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user369 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user369@elastica.co';
        http_headers = [user369: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R370 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user370 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user370@elastica.co';
        http_headers = [user370: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R371 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user371 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user371@elastica.co';
        http_headers = [user371: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R372 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user372 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user372@elastica.co';
        http_headers = [user372: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R373 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user373 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user373@elastica.co';
        http_headers = [user373: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R374 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user374 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user374@elastica.co';
        http_headers = [user374: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R375 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user375 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user375@elastica.co';
        http_headers = [user375: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R376 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user376 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user376@elastica.co';
        http_headers = [user376: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R377 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user377 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user377@elastica.co';
        http_headers = [user377: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R378 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user378 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user378@elastica.co';
        http_headers = [user378: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R379 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user379 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user379@elastica.co';
        http_headers = [user379: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R380 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user380 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user380@elastica.co';
        http_headers = [user380: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R381 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user381 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user381@elastica.co';
        http_headers = [user381: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R382 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user382 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user382@elastica.co';
        http_headers = [user382: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R383 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user383 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user383@elastica.co';
        http_headers = [user383: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R384 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user384 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user384@elastica.co';
        http_headers = [user384: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R385 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user385 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user385@elastica.co';
        http_headers = [user385: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R386 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user386 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user386@elastica.co';
        http_headers = [user386: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R387 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user387 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user387@elastica.co';
        http_headers = [user387: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R388 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user388 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user388@elastica.co';
        http_headers = [user388: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R389 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user389 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user389@elastica.co';
        http_headers = [user389: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R390 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user390 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user390@elastica.co';
        http_headers = [user390: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R391 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user391 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user391@elastica.co';
        http_headers = [user391: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R392 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user392 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user392@elastica.co';
        http_headers = [user392: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R393 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user393 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user393@elastica.co';
        http_headers = [user393: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R394 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user394 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user394@elastica.co';
        http_headers = [user394: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R395 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user395 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user395@elastica.co';
        http_headers = [user395: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R396 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user396 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user396@elastica.co';
        http_headers = [user396: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R397 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user397 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user397@elastica.co';
        http_headers = [user397: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R398 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user398 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user398@elastica.co';
        http_headers = [user398: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R399 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user399 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user399@elastica.co';
        http_headers = [user399: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R400 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user400 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user400@elastica.co';
        http_headers = [user400: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R401 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user401 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user401@elastica.co';
        http_headers = [user401: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R402 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user402 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user402@elastica.co';
        http_headers = [user402: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R403 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user403 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user403@elastica.co';
        http_headers = [user403: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R404 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user404 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user404@elastica.co';
        http_headers = [user404: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R405 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user405 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user405@elastica.co';
        http_headers = [user405: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R406 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user406 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user406@elastica.co';
        http_headers = [user406: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R407 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user407 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user407@elastica.co';
        http_headers = [user407: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R408 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user408 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user408@elastica.co';
        http_headers = [user408: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R409 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user409 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user409@elastica.co';
        http_headers = [user409: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R410 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user410 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user410@elastica.co';
        http_headers = [user410: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R411 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user411 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user411@elastica.co';
        http_headers = [user411: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R412 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user412 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user412@elastica.co';
        http_headers = [user412: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R413 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user413 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user413@elastica.co';
        http_headers = [user413: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R414 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user414 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user414@elastica.co';
        http_headers = [user414: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R415 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user415 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user415@elastica.co';
        http_headers = [user415: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R416 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user416 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user416@elastica.co';
        http_headers = [user416: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R417 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user417 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user417@elastica.co';
        http_headers = [user417: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R418 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user418 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user418@elastica.co';
        http_headers = [user418: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R419 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user419 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user419@elastica.co';
        http_headers = [user419: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R420 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user420 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user420@elastica.co';
        http_headers = [user420: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R421 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user421 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user421@elastica.co';
        http_headers = [user421: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R422 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user422 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user422@elastica.co';
        http_headers = [user422: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R423 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user423 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user423@elastica.co';
        http_headers = [user423: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R424 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user424 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user424@elastica.co';
        http_headers = [user424: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R425 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user425 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user425@elastica.co';
        http_headers = [user425: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R426 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user426 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user426@elastica.co';
        http_headers = [user426: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R427 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user427 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user427@elastica.co';
        http_headers = [user427: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R428 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user428 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user428@elastica.co';
        http_headers = [user428: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R429 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user429 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user429@elastica.co';
        http_headers = [user429: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R430 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user430 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user430@elastica.co';
        http_headers = [user430: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R431 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user431 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user431@elastica.co';
        http_headers = [user431: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R432 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user432 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user432@elastica.co';
        http_headers = [user432: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R433 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user433 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user433@elastica.co';
        http_headers = [user433: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R434 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user434 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user434@elastica.co';
        http_headers = [user434: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R435 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user435 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user435@elastica.co';
        http_headers = [user435: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R436 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user436 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user436@elastica.co';
        http_headers = [user436: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R437 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user437 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user437@elastica.co';
        http_headers = [user437: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R438 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user438 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user438@elastica.co';
        http_headers = [user438: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R439 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user439 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user439@elastica.co';
        http_headers = [user439: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R440 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user440 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user440@elastica.co';
        http_headers = [user440: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R441 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user441 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user441@elastica.co';
        http_headers = [user441: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R442 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user442 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user442@elastica.co';
        http_headers = [user442: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R443 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user443 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user443@elastica.co';
        http_headers = [user443: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R444 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user444 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user444@elastica.co';
        http_headers = [user444: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R445 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user445 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user445@elastica.co';
        http_headers = [user445: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R446 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user446 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user446@elastica.co';
        http_headers = [user446: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R447 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user447 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user447@elastica.co';
        http_headers = [user447: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R448 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user448 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user448@elastica.co';
        http_headers = [user448: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R449 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user449 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user449@elastica.co';
        http_headers = [user449: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R450 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user450 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user450@elastica.co';
        http_headers = [user450: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R451 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user451 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user451@elastica.co';
        http_headers = [user451: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R452 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user452 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user452@elastica.co';
        http_headers = [user452: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R453 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user453 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user453@elastica.co';
        http_headers = [user453: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R454 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user454 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user454@elastica.co';
        http_headers = [user454: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R455 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user455 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user455@elastica.co';
        http_headers = [user455: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R456 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user456 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user456@elastica.co';
        http_headers = [user456: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R457 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user457 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user457@elastica.co';
        http_headers = [user457: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R458 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user458 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user458@elastica.co';
        http_headers = [user458: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R459 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user459 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user459@elastica.co';
        http_headers = [user459: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R460 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user460 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user460@elastica.co';
        http_headers = [user460: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R461 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user461 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user461@elastica.co';
        http_headers = [user461: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R462 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user462 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user462@elastica.co';
        http_headers = [user462: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R463 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user463 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user463@elastica.co';
        http_headers = [user463: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R464 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user464 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user464@elastica.co';
        http_headers = [user464: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R465 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user465 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user465@elastica.co';
        http_headers = [user465: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R466 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user466 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user466@elastica.co';
        http_headers = [user466: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R467 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user467 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user467@elastica.co';
        http_headers = [user467: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R468 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user468 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user468@elastica.co';
        http_headers = [user468: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R469 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user469 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user469@elastica.co';
        http_headers = [user469: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R470 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user470 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user470@elastica.co';
        http_headers = [user470: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R471 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user471 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user471@elastica.co';
        http_headers = [user471: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R472 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user472 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user472@elastica.co';
        http_headers = [user472: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R473 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user473 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user473@elastica.co';
        http_headers = [user473: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R474 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user474 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user474@elastica.co';
        http_headers = [user474: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R475 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user475 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user475@elastica.co';
        http_headers = [user475: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R476 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user476 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user476@elastica.co';
        http_headers = [user476: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R477 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user477 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user477@elastica.co';
        http_headers = [user477: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R478 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user478 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user478@elastica.co';
        http_headers = [user478: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R479 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user479 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user479@elastica.co';
        http_headers = [user479: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R480 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user480 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user480@elastica.co';
        http_headers = [user480: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R481 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user481 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user481@elastica.co';
        http_headers = [user481: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R482 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user482 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user482@elastica.co';
        http_headers = [user482: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R483 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user483 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user483@elastica.co';
        http_headers = [user483: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R484 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user484 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user484@elastica.co';
        http_headers = [user484: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R485 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user485 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user485@elastica.co';
        http_headers = [user485: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R486 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user486 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user486@elastica.co';
        http_headers = [user486: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R487 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user487 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user487@elastica.co';
        http_headers = [user487: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R488 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user488 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user488@elastica.co';
        http_headers = [user488: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R489 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user489 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user489@elastica.co';
        http_headers = [user489: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R490 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user490 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user490@elastica.co';
        http_headers = [user490: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R491 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user491 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user491@elastica.co';
        http_headers = [user491: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R492 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user492 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user492@elastica.co';
        http_headers = [user492: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R493 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user493 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user493@elastica.co';
        http_headers = [user493: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R494 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user494 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user494@elastica.co';
        http_headers = [user494: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R495 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user495 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user495@elastica.co';
        http_headers = [user495: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R496 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user496 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user496@elastica.co';
        http_headers = [user496: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R497 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user497 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user497@elastica.co';
        http_headers = [user497: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R498 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user498 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user498@elastica.co';
        http_headers = [user498: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R499 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user499 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user499@elastica.co';
        http_headers = [user499: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R500 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user500 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user500@elastica.co';
        http_headers = [user500: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R501 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user501 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user501@elastica.co';
        http_headers = [user501: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R502 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user502 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user502@elastica.co';
        http_headers = [user502: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R503 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user503 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user503@elastica.co';
        http_headers = [user503: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R504 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user504 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user504@elastica.co';
        http_headers = [user504: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R505 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user505 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user505@elastica.co';
        http_headers = [user505: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R506 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user506 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user506@elastica.co';
        http_headers = [user506: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R507 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user507 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user507@elastica.co';
        http_headers = [user507: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R508 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user508 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user508@elastica.co';
        http_headers = [user508: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R509 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user509 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user509@elastica.co';
        http_headers = [user509: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R510 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user510 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user510@elastica.co';
        http_headers = [user510: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R511 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user511 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user511@elastica.co';
        http_headers = [user511: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R512 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user512 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user512@elastica.co';
        http_headers = [user512: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R513 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user513 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user513@elastica.co';
        http_headers = [user513: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R514 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user514 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user514@elastica.co';
        http_headers = [user514: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R515 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user515 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user515@elastica.co';
        http_headers = [user515: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R516 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user516 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user516@elastica.co';
        http_headers = [user516: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R517 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user517 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user517@elastica.co';
        http_headers = [user517: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R518 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user518 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user518@elastica.co';
        http_headers = [user518: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R519 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user519 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user519@elastica.co';
        http_headers = [user519: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R520 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user520 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user520@elastica.co';
        http_headers = [user520: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R521 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user521 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user521@elastica.co';
        http_headers = [user521: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R522 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user522 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user522@elastica.co';
        http_headers = [user522: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R523 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user523 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user523@elastica.co';
        http_headers = [user523: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R524 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user524 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user524@elastica.co';
        http_headers = [user524: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R525 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user525 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user525@elastica.co';
        http_headers = [user525: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R526 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user526 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user526@elastica.co';
        http_headers = [user526: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R527 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user527 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user527@elastica.co';
        http_headers = [user527: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R528 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user528 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user528@elastica.co';
        http_headers = [user528: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R529 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user529 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user529@elastica.co';
        http_headers = [user529: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R530 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user530 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user530@elastica.co';
        http_headers = [user530: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R531 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user531 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user531@elastica.co';
        http_headers = [user531: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R532 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user532 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user532@elastica.co';
        http_headers = [user532: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R533 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user533 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user533@elastica.co';
        http_headers = [user533: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R534 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user534 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user534@elastica.co';
        http_headers = [user534: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R535 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user535 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user535@elastica.co';
        http_headers = [user535: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R536 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user536 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user536@elastica.co';
        http_headers = [user536: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R537 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user537 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user537@elastica.co';
        http_headers = [user537: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R538 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user538 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user538@elastica.co';
        http_headers = [user538: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R539 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user539 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user539@elastica.co';
        http_headers = [user539: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R540 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user540 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user540@elastica.co';
        http_headers = [user540: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R541 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user541 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user541@elastica.co';
        http_headers = [user541: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R542 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user542 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user542@elastica.co';
        http_headers = [user542: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R543 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user543 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user543@elastica.co';
        http_headers = [user543: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R544 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user544 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user544@elastica.co';
        http_headers = [user544: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R545 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user545 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user545@elastica.co';
        http_headers = [user545: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R546 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user546 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user546@elastica.co';
        http_headers = [user546: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R547 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user547 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user547@elastica.co';
        http_headers = [user547: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R548 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user548 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user548@elastica.co';
        http_headers = [user548: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R549 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user549 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user549@elastica.co';
        http_headers = [user549: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R550 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user550 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user550@elastica.co';
        http_headers = [user550: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R551 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user551 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user551@elastica.co';
        http_headers = [user551: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R552 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user552 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user552@elastica.co';
        http_headers = [user552: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R553 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user553 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user553@elastica.co';
        http_headers = [user553: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R554 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user554 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user554@elastica.co';
        http_headers = [user554: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R555 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user555 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user555@elastica.co';
        http_headers = [user555: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R556 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user556 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user556@elastica.co';
        http_headers = [user556: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R557 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user557 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user557@elastica.co';
        http_headers = [user557: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R558 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user558 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user558@elastica.co';
        http_headers = [user558: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R559 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user559 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user559@elastica.co';
        http_headers = [user559: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R560 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user560 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user560@elastica.co';
        http_headers = [user560: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R561 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user561 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user561@elastica.co';
        http_headers = [user561: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R562 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user562 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user562@elastica.co';
        http_headers = [user562: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R563 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user563 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user563@elastica.co';
        http_headers = [user563: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R564 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user564 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user564@elastica.co';
        http_headers = [user564: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R565 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user565 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user565@elastica.co';
        http_headers = [user565: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R566 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user566 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user566@elastica.co';
        http_headers = [user566: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R567 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user567 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user567@elastica.co';
        http_headers = [user567: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R568 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user568 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user568@elastica.co';
        http_headers = [user568: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R569 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user569 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user569@elastica.co';
        http_headers = [user569: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R570 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user570 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user570@elastica.co';
        http_headers = [user570: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R571 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user571 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user571@elastica.co';
        http_headers = [user571: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R572 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user572 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user572@elastica.co';
        http_headers = [user572: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R573 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user573 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user573@elastica.co';
        http_headers = [user573: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R574 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user574 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user574@elastica.co';
        http_headers = [user574: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R575 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user575 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user575@elastica.co';
        http_headers = [user575: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R576 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user576 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user576@elastica.co';
        http_headers = [user576: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R577 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user577 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user577@elastica.co';
        http_headers = [user577: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R578 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user578 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user578@elastica.co';
        http_headers = [user578: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R579 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user579 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user579@elastica.co';
        http_headers = [user579: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R580 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user580 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user580@elastica.co';
        http_headers = [user580: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R581 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user581 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user581@elastica.co';
        http_headers = [user581: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R582 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user582 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user582@elastica.co';
        http_headers = [user582: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R583 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user583 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user583@elastica.co';
        http_headers = [user583: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R584 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user584 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user584@elastica.co';
        http_headers = [user584: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R585 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user585 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user585@elastica.co';
        http_headers = [user585: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R586 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user586 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user586@elastica.co';
        http_headers = [user586: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R587 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user587 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user587@elastica.co';
        http_headers = [user587: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R588 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user588 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user588@elastica.co';
        http_headers = [user588: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R589 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user589 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user589@elastica.co';
        http_headers = [user589: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R590 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user590 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user590@elastica.co';
        http_headers = [user590: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R591 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user591 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user591@elastica.co';
        http_headers = [user591: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R592 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user592 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user592@elastica.co';
        http_headers = [user592: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R593 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user593 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user593@elastica.co';
        http_headers = [user593: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R594 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user594 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user594@elastica.co';
        http_headers = [user594: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R595 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user595 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user595@elastica.co';
        http_headers = [user595: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R596 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user596 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user596@elastica.co';
        http_headers = [user596: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R597 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user597 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user597@elastica.co';
        http_headers = [user597: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R598 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user598 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user598@elastica.co';
        http_headers = [user598: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R599 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user599 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user599@elastica.co';
        http_headers = [user599: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
Robot R600 = {

        req_methods = [ "GET" ];
	pop_model = { pop_distr = popUnif(); };
        recurrence = 55% / SimpleContent.cachable; // adjusted to get 55% DHR
        origins = map.names; 
	//origins = S.addresses;     // where the origin servers are
        dns_resolver = dr;
        ssl_wraps = [ wrap ];
        
        MimeHeader user600 = 'ELASTICA_MAGIC_COOKIE: 279604628030:stresstest.user600@elastica.co';
        http_headers = [user600: 100%];
        addresses = ['10.0.15.105' ** 1 ]; // where these robot agents will be created
};
// commit to using these servers and robots
use(S,R1,R2,R3,R4,R5,R6,R7,R8,R9,R10,R11,R12,R13,R14,R15,R16,R17,R18,R19,R20,R21,R22,R23,R24,R25,R26,R27,R28,R29,R30,R31,R32,R33,R34,R35,R36,R37,R38,R39,R40,R41,R42,R43,R44,R45,R46,R47,R48,R49,R50,R51,R52,R53,R54,R55,R56,R57,R58,R59,R60,R61,R62,R63,R64,R65,R66,R67,R68,R69,R70,R71,R72,R73,R74,R75,R76,R77,R78,R79,R80,R81,R82,R83,R84,R85,R86,R87,R88,R89,R90,R91,R92,R93,R94,R95,R96,R97,R98,R99,R100,R101,R102,R103,R104,R105,R106,R107,R108,R109,R110,R111,R112,R113,R114,R115,R116,R117,R118,R119,R120,R121,R122,R123,R124,R125,R126,R127,R128,R129,R130,R131,R132,R133,R134,R135,R136,R137,R138,R139,R140,R141,R142,R143,R144,R145,R146,R147,R148,R149,R150,R151,R152,R153,R154,R155,R156,R157,R158,R159,R160,R161,R162,R163,R164,R165,R166,R167,R168,R169,R170,R171,R172,R173,R174,R175,R176,R177,R178,R179,R180,R181,R182,R183,R184,R185,R186,R187,R188,R189,R190,R191,R192,R193,R194,R195,R196,R197,R198,R199,R200,R201,R202,R203,R204,R205,R206,R207,R208,R209,R210,R211,R212,R213,R214,R215,R216,R217,R218,R219,R220,R221,R222,R223,R224,R225,R226,R227,R228,R229,R230,R231,R232,R233,R234,R235,R236,R237,R238,R239,R240,R241,R242,R243,R244,R245,R246,R247,R248,R249,R250,R251,R252,R253,R254,R255,R256,R257,R258,R259,R260,R261,R262,R263,R264,R265,R266,R267,R268,R269,R270,R271,R272,R273,R274,R275,R276,R277,R278,R279,R280,R281,R282,R283,R284,R285,R286,R287,R288,R289,R290,R291,R292,R293,R294,R295,R296,R297,R298,R299,R300,R301,R302,R303,R304,R305,R306,R307,R308,R309,R310,R311,R312,R313,R314,R315,R316,R317,R318,R319,R320,R321,R322,R323,R324,R325,R326,R327,R328,R329,R330,R331,R332,R333,R334,R335,R336,R337,R338,R339,R340,R341,R342,R343,R344,R345,R346,R347,R348,R349,R350,R351,R352,R353,R354,R355,R356,R357,R358,R359,R360,R361,R362,R363,R364,R365,R366,R367,R368,R369,R370,R371,R372,R373,R374,R375,R376,R377,R378,R379,R380,R381,R382,R383,R384,R385,R386,R387,R388,R389,R390,R391,R392,R393,R394,R395,R396,R397,R398,R399,R400,R401,R402,R403,R404,R405,R406,R407,R408,R409,R410,R411,R412,R413,R414,R415,R416,R417,R418,R419,R420,R421,R422,R423,R424,R425,R426,R427,R428,R429,R430,R431,R432,R433,R434,R435,R436,R437,R438,R439,R440,R441,R442,R443,R444,R445,R446,R447,R448,R449,R450,R451,R452,R453,R454,R455,R456,R457,R458,R459,R460,R461,R462,R463,R464,R465,R466,R467,R468,R469,R470,R471,R472,R473,R474,R475,R476,R477,R478,R479,R480,R481,R482,R483,R484,R485,R486,R487,R488,R489,R490,R491,R492,R493,R494,R495,R496,R497,R498,R499,R500,R501,R502,R503,R504,R505,R506,R507,R508,R509,R510,R511,R512,R513,R514,R515,R516,R517,R518,R519,R520,R521,R522,R523,R524,R525,R526,R527,R528,R529,R530,R531,R532,R533,R534,R535,R536,R537,R538,R539,R540,R541,R542,R543,R544,R545,R546,R547,R548,R549,R550,R551,R552,R553,R554,R555,R556,R557,R558,R559,R560,R561,R562,R563,R564,R565,R566,R567,R568,R569,R570,R571,R572,R573,R574,R575,R576,R577,R578,R579,R580,R581,R582,R583,R584,R585,R586,R587,R588,R589,R590,R591,R592,R593,R594,R595,R596,R597,R598,R599,R600);
